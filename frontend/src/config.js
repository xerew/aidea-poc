// Feature flags. Flip a flag to show/hide a feature without deleting its code.

// Direct messaging between users. Set to true to re-enable the Messages page,
// the sidebar entry, the profile "Send message" button and unread polling.
export const MESSAGING_ENABLED = false
